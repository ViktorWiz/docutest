name: Simple Usage

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: docker://pandoc/core:2.9
        with:
          args: "docs/doc1.md -s -o docs/doc1.pdf" # gets appended to pandoc command
